# NOTE: This file is a placeholder. In real environments secrets are mounted into the pod via
# the Azure Key Vault CSI driver (see secretproviderclass.yaml). Never check real values in.
apiVersion: v1
kind: Secret
metadata:
  name: reviews-secret
  namespace: freshcart
type: Opaque
stringData:
  ConnectionStrings__reviewsdb: 'mongodb://freshcart-dev-cosmos.mongo.cosmos.azure.com:10255/freshcart_reviews?ssl=true&retrywrites=false'
