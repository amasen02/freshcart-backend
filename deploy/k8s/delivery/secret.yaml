# NOTE: This file is a placeholder. In real environments secrets are mounted into the pod via
# the Azure Key Vault CSI driver (see secretproviderclass.yaml). Never check real values in.
apiVersion: v1
kind: Secret
metadata:
  name: delivery-secret
  namespace: freshcart
type: Opaque
stringData:
  ConnectionStrings__deliverydb: 'mongodb://freshcart-dev-cosmos.mongo.cosmos.azure.com:10255/freshcart_delivery?ssl=true&retrywrites=false'
