apiVersion: v1
kind: ServiceAccount
metadata:
  name: delivery
  namespace: freshcart
  annotations:
    # Replace with the federated identity client id for the environment.
    azure.workload.identity/client-id: '00000000-0000-0000-0000-000000000000'
  labels:
    azure.workload.identity/use: 'true'
