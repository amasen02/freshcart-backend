apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: notification
  namespace: freshcart
spec:
  selector:
    matchLabels: { app.kubernetes.io/name: notification }
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
