apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: customersupport
  namespace: freshcart
spec:
  minAvailable: 1
  selector:
    matchLabels: { app.kubernetes.io/name: customersupport }
