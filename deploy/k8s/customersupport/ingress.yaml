apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: customersupport
  namespace: freshcart
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: '8m'
    nginx.ingress.kubernetes.io/proxy-read-timeout: '60'
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts: [customersupport.freshcart.local]
      secretName: customersupport-tls
  rules:
    - host: customersupport.freshcart.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: customersupport
                port: { number: 80 }
