# NOTE: This file is a placeholder. In real environments secrets are mounted into the pod via
# the Azure Key Vault CSI driver (see secretproviderclass.yaml). Never check real values in.
apiVersion: v1
kind: Secret
metadata:
  name: inventory-secret
  namespace: freshcart
type: Opaque
stringData:
  ConnectionStrings__inventorydb: 'Server=tcp:freshcart-dev-sql.database.windows.net,1433;Database=FreshCart.Inventory;Authentication=Active Directory Workload Identity;Encrypt=True'
