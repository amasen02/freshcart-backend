# NOTE: Pricing has no network secrets. Its only datastore is the embedded SQLite
# file, configured as plain data in configmap.yaml. This empty Secret exists only
# so the Deployment's secretRef resolves; real services populate it from Key Vault.
apiVersion: v1
kind: Secret
metadata:
  name: pricing-secret
  namespace: freshcart
type: Opaque
stringData: {}
