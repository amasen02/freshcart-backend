apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "delivery.fullname" . }}-config
  labels: {{- include "delivery.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
