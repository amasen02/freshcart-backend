apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "customersupport.fullname" . }}-config
  labels: {{- include "customersupport.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
