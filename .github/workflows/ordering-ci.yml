# ---------------------------------------------------------------------------
# CI for the ordering service: restore, build (Release) and test on master.
# Build + test only — no deploy. Deployment lives in deploy/helm + infra/.
# ---------------------------------------------------------------------------
name: ordering-ci

on:
  push:
    branches: [master]
    paths:
      - 'src/Services/Ordering/**'
      - 'src/BuildingBlocks/**'
      - '.github/workflows/ordering-ci.yml'
      - '.github/workflows/reusable-build-test.yml'
      - '.github/workflows/reusable-docker-security.yml'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - 'global.json'
  pull_request:
    branches: [master]
    paths:
      - 'src/Services/Ordering/**'
      - 'src/BuildingBlocks/**'
  workflow_dispatch:

concurrency:
  group: ordering-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build · Test
    uses: ./.github/workflows/reusable-build-test.yml
    with:
      serviceName: ordering
      testProjectPath: src/Services/Ordering/FreshCart.Ordering.Tests/FreshCart.Ordering.Tests.csproj

  docker-security:
    name: Docker security
    uses: ./.github/workflows/reusable-docker-security.yml
    permissions:
      contents: read
      packages: write
      id-token: write
    with:
      serviceName: ordering
      dockerfilePath: src/Services/Ordering/FreshCart.Ordering.Api/Dockerfile
