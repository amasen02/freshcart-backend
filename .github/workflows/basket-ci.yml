# ---------------------------------------------------------------------------
# CI for the basket service: restore, build (Release) and test on master.
# Build + test only — no deploy. Deployment lives in deploy/helm + infra/.
# ---------------------------------------------------------------------------
name: basket-ci

on:
  push:
    branches: [master]
    paths:
      - 'src/Services/Basket/**'
      - 'src/BuildingBlocks/**'
      - '.github/workflows/basket-ci.yml'
      - '.github/workflows/reusable-build-test.yml'
      - '.github/workflows/reusable-docker-security.yml'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - 'global.json'
  pull_request:
    branches: [master]
    paths:
      - 'src/Services/Basket/**'
      - 'src/BuildingBlocks/**'
  workflow_dispatch:

concurrency:
  group: basket-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build · Test
    uses: ./.github/workflows/reusable-build-test.yml
    with:
      serviceName: basket
      testProjectPath: src/Services/Basket/FreshCart.Basket.Tests/FreshCart.Basket.Tests.csproj

  docker-security:
    name: Docker security
    uses: ./.github/workflows/reusable-docker-security.yml
    permissions:
      contents: read
      packages: write
      id-token: write
    with:
      serviceName: basket
      dockerfilePath: src/Services/Basket/FreshCart.Basket.Api/Dockerfile
