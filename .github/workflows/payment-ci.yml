# ---------------------------------------------------------------------------
# Per-service CI/CD for the Payment bounded context.
# Same job graph as identity-ci.yml: build-test → container → deploy-dev →
# deploy-staging → deploy-prod, parameterised for Payment.
# ---------------------------------------------------------------------------
name: payment-ci

on:
  push:
    branches: [main]
    paths:
      - 'src/Services/Payment/**'
      - 'src/BuildingBlocks/**'
      - '.github/workflows/payment-ci.yml'
      - '.github/workflows/reusable-*.yml'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - 'global.json'
  pull_request:
    branches: [main]
    paths:
      - 'src/Services/Payment/**'
      - 'src/BuildingBlocks/**'
  workflow_dispatch:

concurrency:
  group: payment-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: Build · Test · Quality
    uses: ./.github/workflows/reusable-build-test.yml
    with:
      serviceName: payment
      projectPath: src/Services/Payment/FreshCart.Payment.Api/FreshCart.Payment.Api.csproj
      testProjectPath: src/Services/Payment/FreshCart.Payment.Tests/FreshCart.Payment.Tests.csproj
      sonarProjectKey: FreshCart_Payment
      sonarOrganization: freshcart
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  container:
    name: Container · Scan · Sign · Push
    needs: build-test
    if: github.event_name != 'pull_request'
    uses: ./.github/workflows/reusable-container.yml
    with:
      serviceName: payment
      dockerfilePath: src/Services/Payment/FreshCart.Payment.Api/Dockerfile
      imageRepository: ${{ github.repository_owner }}/freshcart-payment
      acrName: freshcartacr
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

  deploy-dev:
    name: Deploy → DEV
    needs: container
    if: github.ref == 'refs/heads/main'
    environment:
      name: freshcart-dev
      url: https://payment-dev.freshcart.local
    uses: ./.github/workflows/reusable-helm-deploy.yml
    with:
      serviceName: payment
      chartPath: deploy/helm/payment
      valuesFile: deploy/helm/payment/values-dev.yaml
      imageTag: ${{ github.sha }}
      resourceGroup: freshcart-dev
      aksClusterName: freshcart-dev-aks
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

  deploy-staging:
    name: Deploy → STAGING (1 approver)
    needs: deploy-dev
    environment:
      name: freshcart-staging
      url: https://payment-staging.freshcart.local
    uses: ./.github/workflows/reusable-helm-deploy.yml
    with:
      serviceName: payment
      chartPath: deploy/helm/payment
      valuesFile: deploy/helm/payment/values-staging.yaml
      imageTag: ${{ github.sha }}
      resourceGroup: freshcart-staging
      aksClusterName: freshcart-staging-aks
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

  deploy-prod:
    name: Deploy → PROD (blue/green, 2 approvers)
    needs: deploy-staging
    environment:
      name: freshcart-prod
      url: https://payment.freshcart.com
    uses: ./.github/workflows/reusable-helm-deploy.yml
    with:
      serviceName: payment
      chartPath: deploy/helm/payment
      valuesFile: deploy/helm/payment/values-prod.yaml
      imageTag: ${{ github.sha }}
      resourceGroup: freshcart-prod
      aksClusterName: freshcart-prod-aks
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
